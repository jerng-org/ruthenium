'use strict'

//  LITERATURE REVIEW:
//  -   https://jakearchibald.com/2017/es-modules-in-browsers/


//  RUNS WHEN (document)'s markup has parsed
document.addEventListener('DOMContentLoaded', (event) => {
    
})

//  RUNS WHEN (document)'s dependencies have all loaded (images, scripts, etc.)
window.addEventListener('load', (event) => {

    
});

window.removeClosest 
    = function (  selector ) {  this.closest( selector ).remove() }

/*
window.RU = {

    removeClosestTr : thisArg => removeClosest ( thisArg, 'tr' )

}*/